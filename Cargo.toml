[package]
name = "no-swear"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
ffmpeg-next = "8"
reqwest = { version = "0.12", features = ["blocking"] }
